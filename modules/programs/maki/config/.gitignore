# Sidecar lock file maki creates while writing the lockfile. The lockfile
# itself (pack-lock.json) IS committed: it pins package revisions.
pack-lock.json.lock

# Generated by modules/programs/maki/default.nix from programs.mcp.servers.
mcp.toml
